const apartmentRoute = require('express').Router();
import 'dotenv/config';
import * as model from '../models.js'


// route to add an apartment
apartmentRoute.post('/', (req, res) => {
    
})


// route to get an apartment
